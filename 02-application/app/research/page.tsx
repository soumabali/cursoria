export const metadata={title:'Storefront research & decisions',robots:{index:false,follow:false}};
export default function Research(){return <article className="content-page prose" dangerouslySetInnerHTML={{__html:'<h1>Cursor Studio: storefront research and implementation decisions</h1><p>Prepared for Wayan Sudharmika · 8 September 2026 · English storefront for an Indonesian-rupiah digital marketplace.</p><h2>Recommendation</h2><p>Build a product-led storefront with public, server-rendered product pages, clear minimum donation pricing, an email verification step at acquisition, and an account library for delivery. Put the cursor itself ahead of promotional decoration: show a recognizable preview, let buyers compare light and dark surfaces, and state compatibility and license close to the purchase button. Use one Midtrans merchant account and isolate each creator’s records on the server. The design should feel playful and considered without inventing popularity, ratings, discounts, or urgency.</p><p>This research supports design choices, not a conversion forecast. There has been no study of this store’s actual customers, no live payment test, and no claim of guaranteed AI citation or ranking. Official technical guidance is the strongest evidence for search and payment behavior; general ecommerce research is a directional input for this specialist product.</p><h2>1. GEO and AEO: make the product understandable</h2><p>Google states that established SEO practices apply to AI Overviews and AI Mode. Important information should be available as text, pages should be discoverable through internal links, and structured data should agree with the visible page. Eligibility does not guarantee indexing or inclusion. There is no requirement for special AI text files or a separate AI schema. For Cursor Studio, the practical decision is to make each published pack a useful answer to a concrete purchase question: what it is, what platform it supports, what files and states it includes, what it costs, and how to install it. <a href="https://developers.google.com/search/docs/appearance/ai-features">Google Search Central: AI features and your website</a>.</p><p>Use one canonical URL per pack, a unique title and description, crawlable internal links, and a sitemap containing published products. Authentication belongs at acquisition and download, not in front of the product description. Private routes must be protected by authorization; robots exclusions are only indexing controls. The preview deployment is deliberately noindex. Enable public indexing only after the real catalog and operator policies are ready.</p><p>Product and Offer JSON-LD can communicate a product’s price and currency. The implementation derives markup from the same database record used by the visible page, uses IDR, and avoids fabricated aggregate ratings. Fixed-price offers use the fixed amount; free offers use zero. A donation pack uses its required minimum as the acquisition price, with visible copy explaining that higher contributions are optional. That donation representation is an implementation inference, not a promise of a particular rich result, and should be checked in the Rich Results Test at launch. <a href="https://developers.google.com/search/docs/appearance/structured-data/product-snippet">Google: Product snippet structured data</a>.</p><p>Visible FAQs are useful for buyers, but FAQ rich results should not be presented as a benefit: Google’s update log says the feature stopped appearing from 7 May 2026. The questions here explain accounts, donations, delivery, and compatibility without relying on FAQ markup. <a href="https://developers.google.com/search/updates">Google Search documentation updates</a>.</p><h2>2. UI: sell the cursor clearly</h2><p>Baymard’s product-page research identifies product pages as central to purchase decisions and describes users abandoning suitable products because of avoidable UX problems. Its evidence comes from broader ecommerce sites, so it should not be converted into a predicted uplift for a cursor marketplace. The relevant implication is to reduce uncertainty around the product, especially on mobile. <a href="https://baymard.com/blog/current-state-ecommerce-product-page-ux">Baymard Institute: Product Page UX Best Practices</a>.</p><p>The chosen interface uses warm off-white surfaces, dark green text, a restrained lime accent, and generous space around previews. The homepage leads with the product’s emotional benefit, then immediately exposes the collection. Cards keep category, creator, cursor count, and price easy to scan. Filter categories are styles rather than unfamiliar file terminology. Search covers pack titles, categories, and creator names. Pricing filters separate free, donation, and fixed-price packs.</p><p>Each detail page places a large preview beside the pack’s name, description, price, and primary action. Light and dark surface controls help buyers inspect contrast; they do not claim to simulate every operating system or the actual animated cursor behavior. Original sample artwork is clearly labeled as a concept and cannot be purchased. Uploaded real preview images replace those concepts when the store is connected.</p><p>Before publishing each real pack, the creator should supply accurate platform support, file formats, state count, version, a meaningful description, a preview of the actual files, and a license. File size and richer animated galleries are sensible later improvements if user feedback shows they are needed; this first implementation does not pretend to display measurements it does not hold.</p><h2>3. UX: verification without hiding the catalog</h2><p>The user explicitly requires verified accounts even for free downloads. The implementation therefore allows discovery without login, then sends an expiring email link when the customer acquires a pack. Confirmation both verifies the address and signs the customer in. When sign-in starts from a product, the stored return path takes the customer back to that product. The email link is consumed only after an explicit confirmation action, reducing accidental consumption by email scanners.</p><p>Pricing language is distinct: “Free,” “From Rp10,000” with an explicit minimum-donation explanation, or a fixed rupiah price. There is no misleading zero-price route for donation products. The donation amount is an integer and the minimum is enforced against the database value on the server. A successful browser redirect is not proof of payment. Order history exposes a payment-status check, and paid or claimed packs appear in My Library.</p><p>The responsive layout changes from three product columns to two, collapses navigation into a labeled mobile menu, and stacks product details. Keyboard focus is visible, fields have labels, controls use semantic elements, and reduced-motion preferences are respected. W3C specifies a 24-by-24-CSS-pixel minimum target-size criterion with exceptions; larger controls are preferable where space permits. This implementation uses larger primary actions, but a full accessibility conformance audit has not been performed. <a href="https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html">W3C: Target Size Minimum</a> and <a href="https://www.w3.org/TR/WCAG22/">WCAG 2.2</a>.</p><h2>4. Payments and delivery</h2><p>Midtrans Snap creation is a backend operation authenticated with the merchant Server Key. The store creates a local order and calculates the amount from stored pricing before requesting checkout. The key never enters customer JavaScript. This implementation uses hosted Snap checkout, so the customer completes payment on Midtrans and returns to the store. <a href="https://docs.midtrans.com/reference/backend-integration">Midtrans: Backend integration</a>.</p><p>Midtrans documents a SHA-512 notification signature over order ID, status code, gross amount, and Server Key. The implementation preserves the original gross-amount string for signature verification, compares the signature in constant time, and also fetches the authoritative transaction status. It checks the returned order ID, IDR currency, and amount before granting access. <a href="https://docs.midtrans.com/reference/handle-notifications">Midtrans: Handle Notifications</a> and <a href="https://docs.midtrans.com/docs/get-status-api-requests">Get Status API</a>.</p><p>Notifications can be duplicated or arrive out of order. The reconciliation statement locks the order, records the event, and changes the entitlement atomically. A late pending state cannot downgrade a paid order; refunded orders remain revoked. Successful settlement or accepted capture may grant access. Full refunds revoke future downloads. The partial-refund review policy is a project decision, rather than a Midtrans requirement. <a href="https://docs.midtrans.com/docs/https-notification-webhooks">Midtrans: HTTPS notifications</a>.</p><p>ZIP files are stored in a private S3-compatible bucket. The download endpoint checks the current verified session and active entitlement before streaming bytes. No permanent public package URL is embedded in the storefront. Upload limits and ZIP entry restrictions reduce exposure, but are not a malware scan. Creators remain responsible for testing their packages; a malware scanning service can be added before a larger public launch.</p><h2>5. Roles and operations</h2><p>Next.js treats Route Handlers and Server Actions as security-sensitive entry points. Hiding a button does not authorize an operation. Cursor Studio checks sessions, verification, roles, and product ownership inside protected server operations. Creators manage their own products and see their own visits and sales; superadmins can manage all products, creator accounts, and the store’s Midtrans settings. <a href="https://nextjs.org/docs/app/guides/authentication">Next.js: Authentication</a> and <a href="https://nextjs.org/docs/app/guides/data-security">Data security</a>.</p><p>The dashboard labels revenue as gross revenue before fees, excludes free claims from paid-order counts, and describes visits as unique visitor/product/day counts. These are not guaranteed human visitors: cleared cookies, bots, and shared devices affect the metric. Creator payouts are manual in this version, with no invented balance or automatic split-payment feature.</p><p>Payment configuration is encrypted with an environment-held AES-256-GCM key. Orders retain their encrypted payment configuration so later settings changes do not prevent reconciliation. The native application uses Next.js and PostgreSQL; the private Sites preview uses the compatible Vinext/Cloudflare build. PostgreSQL is accessed through Neon’s HTTPS SQL endpoint to work in both runtimes. Real deployment requires a PostgreSQL connection, email provider, private file bucket, secret encryption key, and Midtrans configuration.</p><h2>6. Launch measurement and remaining validation</h2><p>Before accepting money: replace sample products; publish the operator identity, support contact, refund process, and data-retention policy; configure a verified email-sending domain; migrate a test PostgreSQL database; test all three pricing modes in Midtrans sandbox; verify duplicate, delayed, failed, and refunded payments; verify downloads and creator isolation with separate accounts; and make the webhook publicly reachable. The owner-private preview cannot receive public Midtrans notifications.</p><p>After public launch, validate the product markup and sitemap in search tools. Measure search impressions and product visits alongside paid orders. Do not label visits-to-orders as a causal conversion measure without an aligned attribution window and consistent visitor definition. Use actual customer questions and support cases to improve product descriptions before adding speculative SEO pages.</p><p>Research stopped after primary sources covered search eligibility, structured offers, payment verification, and server authorization, and specialist research supported the principal product-page and accessibility decisions. Remaining uncertainty is operational and customer-specific: real integrations, browser interaction checks, accessibility review, and observed purchase behavior. No live integration or browser testing is represented as completed in this report.\n</p>'}}/>}
